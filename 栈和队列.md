```C++
//define MAXNUM 1000 /*max number of the stack*/
struct SeqStack
{
    DataType *s; //DataType [MAXNUM]
    int t;  /*指向栈顶位置，空栈则为-1*/
}
typedef struct SeqStack, *PSeqStack;
PSeqStack pastack; /*指向顺序栈的指针变量*/
// pastack->t 指向栈顶的变量
// pastack->s 存放栈中元素的数组
// pastack->s[pastack->t] 当前的栈顶元素
```  
### 顺序栈的基本运算  
```C++
PSeqStack creatEmptyStack_seq(int m)//创建一个空栈; m = MAXNUM
{
    PSeqStack pastack;
    pstack = (PSeqStack)malloc(sizeof(struct SeqStack));
    if(pastack!=NULL)
    {
        pastack->element = (DataType*)malloc(sizeof(DataType)*m);
        if(pastack->element != NULL)
        {
            palist->t = -1; return(pastack);
        }
        else freee(pastack);
    }
    printf("out of space!\n");
    return (NULL);
}
//上溢出：pastack->t >= MAXNUM
//下溢出：pastack->t < -1
//栈满：pastack->t = MAXNUM - 1
//栈空：pastack->t = -1


/*往pastack推入一个x的元素*/
int push_seq(PSeqStack pastack, DataType x)
{
    if(pastack->t >= MAXNUM - 1)
    {
        printf("overflow\n");
        return ERROR;
    }
    else /* push */
    {
        pastack->t = pastack->t + 1; /*先变化指针，再插入*/
        pastack->s[pastack->t] = x;
        return true；
    }
}

/*Delete an element from the stack*/
int pop_seq(PSeqStack pastack)
{
    int temp;
    if(pastack->t == -1)
    {
        printf("Underflow\n");
        return error;
    }
    else
    {
        temp=pastack->s[pastack->t];  pastack->t = pastack->t-1;
         return temp;
    }
}
```
### 链栈基本运算的实现
```c++
struct  Node;                      /*单链表结点结构*/           
  typedef struct Node *PNode; /*指向结点的指针类型 */
  struct Node {
	 DataType  info;		
	 PNode   link;		/* 下一个结点 */
  };

链接栈类型定义 
  struct LinkStack  {        	
         PNode  top;	      /* 栈顶指针，指向栈顶结点 */
  };
  typedef struct LinkStack *PLinkStack;       /*链栈类型的指针类型*/


/* 创建一空链接栈，返回指向空链接栈的指针。 */
PLinkStack CreateEmptyStack( void )
{
    PLinkStack plstack;    
　 plstack = (PLinkStack)malloc( sizeof(struct LinkStack));  /* 申请头指针 */
    if (plstack != NULL)
    {
         plstack->top = NULL;     /* 栈顶指针指向空 */
    }
    else
        printf("空间不够! \n");
    return plstack;
 }

 /* 表示往plstack所指的栈中插入（或称推入）一个值为ｘ的元素。 */
int push_link( PLinkStack plstack, DataType x )
{
    PNode p;　
    p = (PNode)malloc( sizeof( struct Node ) ); /* 申请新结点 */
    if(p==NULL)
    {
        printf("Out of space!\n");          return ERROR;
    }
    else /* 在栈顶插入新结点 */
    {
         p->info = x;     p->link = plstack->top;           plstack->top = p;
        return OK;
    }
}

/* 表示从plstack所指的栈中删除（或称弹出）一个元素。 */ 
void pop_link( PLinkStack plstack)
{
    PNode p;
   if (isEmptyStack_link( plstack ) ) /* 首先判断空栈否？*/
    {
        printf("Empty stack pop. \n");        
    }
    else    /*修改栈顶指针 */
    {
       p = plstack->top;         plstack->top = plstack->top->link;         free(p);
    }
}
```
